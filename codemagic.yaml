workflows:
  ios-workflow:
    name: iOS Workflow
    max_build_duration: 60
    environment:
      xcode: latest
      cocoapods: default
    scripts:
      - name: Install npm dependencies
        script: npm install
      - name: Build web assets
        script: npm run build
      - name: Ensure www directory exists and sync
        script: |
          # Agar build dist me banta hai to use www me copy karne ke liye:
          if [ -d "dist" ]; then cp -r dist/* www/; fi
          npx cap sync ios
    artifacts:
      - build/ios/ipa/*.ipa